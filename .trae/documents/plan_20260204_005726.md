## 问题分析

用户提供了详细的 curl 命令，用于访问 Dedaub 的回调 URL 并获取最终的 cookie。当前的 `loginToDedaub` 方法使用的是硬编码的 cookie，需要实现完整的登录流程，包括：

1. **步骤 1**：访问 Dedaub 登录页面，获取初始 cookie 和 CSRF token
2. **步骤 2**：提交登录表单，处理第一次重定向
3. **步骤 3**：处理第二次重定向，提取最终的 cookie

用户已经提供了详细的 curl 命令，包括请求头和 cookie，我可以根据这些信息实现完整的登录流程。

## 解决方案

修改 `loginToDedaub` 方法，实现完整的登录流程，具体步骤如下：

1. **访问登录页面**：
   - 访问 Dedaub 登录页面，获取初始 cookie 和 CSRF token
   - 提取必要的参数，如 `state`、`code_verifier` 等

2. **提交登录表单**：
   - 构造登录请求，包含用户名、密码和必要的参数
   - 发送登录请求，处理第一次重定向
   - 提取第一次重定向的 URL

3. **处理回调**：
   - 访问回调 URL，处理第二次重定向
   - 提取最终的 cookie，如 `__Secure-next-auth.session-token.0` 和 `__Secure-next-auth.session-token.1`

4. **存储 cookie**：
   - 将提取的 cookie 存储到 `cookieManager` 中
   - 保存 cookie 到文件，确保持久化存储

## 具体实现

1. **文件路径**：
   - `internal/service/getcode/get_code_service.go`：修改 `loginToDedaub` 方法

2. **核心功能**：
   - **loginToDedaub**：实现完整的登录流程，包括访问登录页面、提交登录表单、处理重定向并提取最终的 cookie
   - **extractCookies**：从响应中提取 cookie
   - **extractParam**：从 URL 或 HTML 中提取参数

3. **技术要点**：
   - 使用 `net/http` 发送 HTTP 请求
   - 使用 `net/http/cookiejar` 管理 cookie
   - 处理 HTTP 重定向
   - 解析 HTML 和 URL，提取必要的参数
   - 管理 cookie 的存储和持久化

## 预期结果

修改后，`loginToDedaub` 方法将实现完整的登录流程，能够自动获取 Dedaub 的 cookie，无需硬编码。这样，系统将能够自动处理登录过程，确保获取反编译代码的过程能够顺利完成。